name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build-linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Install cross-compilation dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y gcc-aarch64-linux-gnu
      - uses: actions/setup-go@v6
        with:
          go-version: 'stable'
          cache: false
      - run: go build -ldflags="-s -w -X main.version=${GITHUB_REF_NAME#v}" -o skills-linux-amd64 .
      - run: CGO_ENABLED=1 GOOS=linux GOARCH=arm64 CC=aarch64-linux-gnu-gcc go build -ldflags="-s -w -X main.version=${GITHUB_REF_NAME#v}" -o skills-linux-arm64 .
      - run: ./skills-linux-amd64 completion zsh > _skills
      - uses: actions/upload-artifact@v4
        with:
          name: skills-linux-amd64
          path: skills-linux-amd64
      - uses: actions/upload-artifact@v4
        with:
          name: skills-linux-arm64
          path: skills-linux-arm64
      - uses: actions/upload-artifact@v4
        with:
          name: skills-zsh-completion
          path: _skills

  build-macos-arm:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-go@v6
        with:
          go-version: 'stable'
          cache: false
      - run: go build -ldflags="-s -w -X main.version=${GITHUB_REF_NAME#v}" -o skills-darwin-arm64 .
      - uses: actions/upload-artifact@v4
        with:
          name: skills-darwin-arm64
          path: skills-darwin-arm64

  release:
    needs: [build-linux, build-macos-arm]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          pattern: skills-*
          merge-multiple: true

      - uses: actions/download-artifact@v4
        with:
          name: skills-zsh-completion

      - name: Checksums
        run: sha256sum skills-* _skills > checksums.txt

      - uses: softprops/action-gh-release@v2
        with:
          files: |
            skills-*
            _skills
            checksums.txt
          generate_release_notes: true

  update-homebrew:
    needs: [build-linux, build-macos-arm]
    runs-on: ubuntu-latest
    if: github.repository == 'cagedbird043/skills'
    steps:
      - uses: actions/checkout@v5
        with:
          path: skills-repo

      - uses: actions/download-artifact@v4
        with:
          pattern: skills-*
          merge-multiple: true

      - uses: actions/download-artifact@v4
        with:
          name: skills-zsh-completion

      - name: Compute SHA256
        id: shas
        run: |
          {
            echo "linux=$(sha256sum skills-linux-amd64 | cut -d' ' -f1)"
            echo "linux_arm=$(sha256sum skills-linux-arm64 | cut -d' ' -f1)"
            echo "darwin=$(sha256sum skills-darwin-arm64 | cut -d' ' -f1)"
            echo "completion=$(sha256sum _skills | cut -d' ' -f1)"
          } >> "$GITHUB_OUTPUT"

      - name: Update formula
        env:
          GH_TOKEN: ${{ secrets.GH_PAT }}
        run: |
          set -euo pipefail
          VERSION="${GITHUB_REF_NAME#v}"

          gh repo clone cagedbird043/homebrew-tap
          cd homebrew-tap
          gh auth setup-git
          python3 "$GITHUB_WORKSPACE/skills-repo/.github/scripts/update-homebrew-formula.py" \
            Formula/skills-cli.rb "$VERSION" \
            "${{ steps.shas.outputs.linux }}" \
            "${{ steps.shas.outputs.darwin }}" \
            "${{ steps.shas.outputs.completion }}" \
            "${{ steps.shas.outputs.linux_arm }}"

          if git diff --quiet; then
            echo "No changes — formula already up to date"
            exit 0
          fi

          git add Formula/skills-cli.rb
          git -c user.name="github-actions[bot]" \
              -c user.email="41898282+github-actions[bot]@users.noreply.github.com" \
              commit -m "skills-cli: update to v${VERSION}"
          git push